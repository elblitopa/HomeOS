# Dependencias de HomeOS Cloud (la imagen Docker de la VM).
# Es requirements.txt MENOS psutil: en cloud las apps se manejan por el
# Windows Agent y process_manager jamás se ejecuta (su import es lazy).
fastapi>=0.115
uvicorn[standard]>=0.32
sqlalchemy>=2.0
python-multipart>=0.0.12
openpyxl>=3.1
pillow>=10.0
# Solo para modo postgres (DATABASE_URL=postgresql+psycopg://...):
#   pip install "psycopg[binary]>=3.1"
